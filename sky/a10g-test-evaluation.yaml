name: inzwa-a10g-test-evaluation

resources:
  cloud: aws
  region: us-east-1
  instance_type: g5.xlarge
  accelerators: A10G:1
  disk_size: 100

workdir: .

envs:
  AWS_REGION: us-east-1
  PYTORCH_CUDA_ALLOC_CONF: expandable_segments:True
  MODEL_URI: s3://teleagents-research-102431378819-us-east-1/models/asr/inzwa/adaptation/fleurs-adaptation-lr3e6-8bit-20260911a/inzwa-parakeet-tdt-0.6b-v3.nemo
  MODEL_SHA256: f6510ce38ebb9071eadce047afe7b4964c846833e5a8afc0c87c6d1bdbf1de1f
  OUTPUT_PREFIX: s3://teleagents-research-102431378819-us-east-1/models/asr/inzwa/evaluations/fleurs-adaptation-step5750-20260911a/full-test-v1

setup: |
  curl -LsSf https://astral.sh/uv/install.sh | sh
  source "$HOME/.local/bin/env"
  uv sync --frozen --extra train
  uv run python -c "import nemo.collections.asr; import pyarrow"

run: |
  set -euo pipefail
  source "$HOME/.local/bin/env"
  uv sync --frozen --extra train
  uv run python -c "from pathlib import Path; import boto3; path=Path.home() / 'inzwa-test-step5750-v1'; path.mkdir(parents=True, exist_ok=True); print({'work_root': str(path), 'aws_account': boto3.client('sts').get_caller_identity()['Account']})"
  uv run inzwa-evaluate \
    --model-uri "$MODEL_URI" \
    --model-sha256 "$MODEL_SHA256" \
    --work-root "$HOME/inzwa-test-step5750-v1" \
    --output-prefix "$OUTPUT_PREFIX" \
    --batch-size 16
